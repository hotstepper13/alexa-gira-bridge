language: java
jdk:
  - oraclejdk8

deploy:
  provider: releases
  api_key: $GH_TOKEN
  file: 
    - "target/gira-bridge-generate-artifacts.tar.gz"
    - "target/gira-bridge-generate-artifacts.zip"
  skip_cleanup: true
  on:
    tags: true
